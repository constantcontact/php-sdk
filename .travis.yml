language: php

php:
  - 7.0
  - 7.1
  - 7.2
  - 7.3
  - 7.4
  - nightly
  - hhvm

install:
  - travis_retry composer update --no-interaction --prefer-source

matrix:
  allow_failures:
    - php: hhvm
    - php: nightly
  fast_finish: true